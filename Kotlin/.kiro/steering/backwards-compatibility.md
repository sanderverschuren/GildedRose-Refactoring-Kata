---
inclusion: always
---

# Backwards Compatibility

- The `Item` class cannot be changed
- The `items` property in `GildedRose` cannot be changed
- The method `UpdateQuality` can be made static if needed
- The `items` property can be made static if needed
